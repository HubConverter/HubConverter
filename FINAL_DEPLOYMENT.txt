SHORTCUTHUB FINAL V3
====================

Local:
- Backend: cd server && npm start
- Frontend: cd client && npm run dev
- Health: http://localhost:4000/api/health

Temporary public test:
cloudflared tunnel --url http://localhost:5173

Render:
Build Command: npm install && npm run build
Start Command: npm start
Health Check Path: /api/health

Important:
Before a real public launch with real users, replace local SQLite persistence with a durable production database and use secure production secrets/environment variables.
