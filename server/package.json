{
  "name": "shortcut-hub-final",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm install && npm --prefix src/client install && npm --prefix src/client run build",
    "start": "node src/index.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2"
  }
}
